{
  "name": "Node.js & TypeScript & Terraform",
  "build": {
    "dockerfile": "Dockerfile"
  },
  "customizations": {
    "vscode": {
      "extensions": ["hashicorp.terraform"]
    }
  },
  "postCreateCommand": "npm install && terraform --version"
}
